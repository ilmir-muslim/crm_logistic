# Core Django
Django==5.2.8
asgiref==3.11.0
sqlparse==0.5.4
pytz==2025.2
tzdata==2025.2

# Database (SQLite included by default)
# PostgreSQL: psycopg2-binary
# MySQL: mysqlclient

# Authentication & Security
# (included in Django)

# Filters and Forms
django-filter==25.2

# Images and QR codes
Pillow==12.0.0  # Updated version
qrcode==8.2

# PDF Generation
reportlab==4.4.5  # For basic PDF
weasyprint==67.0  # For HTML to PDF
pydyf==0.12.1
cssselect2==0.8.0
html5lib==1.1
tinycss2==1.5.1
webencodings==0.5.1

# Excel Export
openpyxl==3.1.5
et_xmlfile==2.0.0
pandas==2.3.3
numpy==2.3.5
python-dateutil==2.9.0.post0

# HTTP requests (for potential API integrations)
requests==2.32.5
certifi==2025.11.12
charset-normalizer==3.4.4
urllib3==2.6.1
idna==3.11

# Utilities
six==1.17.0

ipython==8.25.0
django-extensions==3.2.3
django-debug-toolbar==4.4.4
python-dotenv==1.0.1

# System dependencies for WeasyPrint (via pip, not system packages)
# Note: On Ubuntu/Debian, also need system packages:
# sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info